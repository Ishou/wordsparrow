import { Lockup } from '@bliss/frontend';

export const Hero = () => <Lockup size="hero" />;

export const Desktop = () => <Lockup size="desktop" />;

export const Mobile = () => <Lockup size="mobile" />;
