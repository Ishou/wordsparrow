{{/*
No Secret manifest is rendered. ADR-0009 §10: secrets are bootstrapped
out-of-band via `kubectl create secret` (recipe in docs/deploy.md);
chart values are repo-committed and "secrets never in code" forbids
embedding secret material (MOLLIE_API_KEY). This file carries the
ServiceAccount the deployment references.
*/}}
{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bliss-billing-api.serviceAccountName" . }}
  labels: {{- include "bliss-billing-api.labels" . | nindent 4 }}
{{- end }}
