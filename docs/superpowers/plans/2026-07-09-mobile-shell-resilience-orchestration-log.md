# Mobile Shell Resilience — Orchestration Log

Append-only log of decisions the orchestrator made during this rollout. For human review when convenient.

## Standing decisions

| Decision | Value | Rationale |
|---|---|---|
| Merge authority | Orchestrator merges on `LGTM` + green CI | Maintainer grant in-session (2026-07-09) |
| On-device gates | **None mid-rollout** — maintainer reviews on-device after Phase 5 | Maintainer chose full autonomy; recorded risk: the target bug is not CI-reproducible, so no mechanical mid-rollout verification |
| 400-line soft target | Orchestrator may invoke §4 override proactively; Phase 1 pre-flagged | Maintainer grant in-session; ADR-0001 §4 2026-05-25 amendment |
| Polling cadence | 120 s (`*/2 * * * *` CronCreate) | Matches prior rollouts |
| Continuity | `CronCreate` (session-only in practice; `durable` flag ignored by runtime) | Known runtime limitation |
| Fix-cycle budget per phase | 3 | dispatch-skill default |
| Phase order | Strictly sequential (0→1→2→3→4→5); Phase 0 blocks Phase 1 (§7); Phases 2–5 depend on AppShell on main; Phase 5 deletes PhoneShell last | ADR-0001 §7 + file dependency |
| Escalation trigger | 3 failed fix-cycles, a CLOSED-unmerged PR, or a non-cap identical-finding loop | Stops chain; logs ACTION + CronDelete |

## Pre-orchestration state

- Spec + plan + this procedure/log live on branch `docs/mobile-shell-resilience`
  (holding branch); the cron reads the procedure from
  `origin/docs/mobile-shell-resilience` until that docs PR merges to `main`.
- No uncommitted local mods or stashes relevant to recovery at bootstrap time.
- The original working branch (`cycle3`) is stale (43 commits behind main at
  session start); all rollout work branches off `origin/main`.

## Event log

- 2026-07-09 tick 1 · Phase 0 · dispatched · implementer for ADR-0072 amend on branch `docs/adr-0072-app-shell` (no PR yet; first phase). Phases 1–5 waiting (depend on Phase 0 / AppShell on main). Docs PR #1492 open (holding branch).
- 2026-07-09 tick 5 · Phase 0 · merged · PR #1493 (ADR-0072 amend) squash-merged; all blocking checks green, §6a review "LGTM, no findings." Phase 1 unblocked — dispatches next tick.
- 2026-07-09 tick 5 · NOTE · dispatching an isolation:worktree agent from the orchestrator's own worktree moved this worktree's HEAD onto the agent branch (`docs/adr-0072-app-shell`, locked). Recovered via unlock + `git checkout docs/mobile-shell-resilience` (branch ref intact on origin, no loss). Mitigation: write+push log BEFORE dispatching agents; checkout-guard the log branch each tick.
- 2026-07-09 tick 6 · CORRECTION · the repo's automated §6a bot pushed `0b7ecd0c` to the #1492 branch: Phase 0 amended the WRONG ADR. Verified against the registry — ADR-0072 = design-system-v2; **ADR-0054 (`0054-page-shell-primitive.md`) binds `frontend/src/ui/**`** (INDEX line 76) and is the correct governing ADR. Root cause: plan Task 1 hardcoded 0072; the tick's `adr-context.sh` ran on the ADR files (empty) not on `frontend/src/ui/v2/AppShell.tsx`, so the miss wasn't caught. #1493 already merged the misfiled 19-line amendment into ADR-0072 (main line 104) + a wrong INDEX row (line 155 AppShell→0072). Corrective Phase 0 (re-dispatch on `docs/adr-0054-app-shell`) will add the amendment to ADR-0054 AND revert both the ADR-0072 section and the wrong INDEX row. Phase 1 depends on this landing.
- 2026-07-09 · RECONCILIATION (§6a cycle 2, PR #1492) · Confirms tick 6's finding independently via §6a review. Also flags that the previously recorded §6a "LGTM, no findings" at tick 5 was itself wrong — its own review text asserted a false non-conflict with ADR-0054. `docs/superpowers/plans/2026-07-09-mobile-shell-resilience.md` Task 1 now carries an explicit Step 0 with the concrete revert commands (files, git log check, commit message) for the corrective Phase 0 dispatch, complementing the procedure's re-targeted Phase 0 row above.
- 2026-07-09 tick 10 · Phase 0 (corrected) · **merged** · PR #1495 squash-merged; LGTM + all blocking checks green. Verified on main: ADR-0072 AppShell section removed, ADR-0054 carries the amendment, INDEX binds AppShell→0054. RECONCILIATION above now resolved — Phase 0 done against the correct ADR; **Phase 1 unblocked**. Worktree-hijack root cause confirmed by the agent: `isolation:"worktree"` assigned the agent the orchestrator's own worktree path; both self-recover, no data loss across two dispatches.
- 2026-07-09 tick 11 · Phase 1 · dispatched · implementer on `feat/frontend-app-shell` (Tasks 2–5: document lock in index.css + AppShell primitive flow/overlay + tests + PhoneShell shim/BottomNav). Cap-heavy → §4 soft-target override pre-flagged from first push. ADR context inlined: ADR-0054 (amended page-shell), ADR-0050 (a11y), ADR-0088 (dark-mode theming vars).
- 2026-07-09 tick ~19 · Phase 1 · intervention · agent idle ~8 min at Task 4 tip (Tasks 2–4 committed: doc lock + AppShell flow + overlay), nothing pushed, no active test process, staged uncommitted index.css. Sent SendMessage nudge to reconcile the staged change, finish Task 5 (PhoneShell shim + BottomNav), run gates, push + open PR, or report the blocker. Message queued (agent loop still alive). Not escalating — work is sound and nearly complete.
- 2026-07-09 tick ~20 · Phase 1 · CORRECTION (stall was a misdiagnosis) · agent is HEALTHY: all four tasks committed (tip `553a7b44` = Task 5 PhoneShell shim + BottomNav) and it's actively running the Playwright e2e gates (live `playwright test` + headless-chromium procs in wt-app-shell) — the static transcript was just the agent blocked on that long subprocess. Prior tick's "wedged" read came from a too-narrow `ps` grep. The nudge (queued) and the precautionary `git push origin feat/frontend-app-shell` (now at 553a7b44) are both benign; the agent will fast-forward + open the PR after gates pass. Lesson: transcript-mtime is an unreliable liveness signal; check for live test procs before concluding a stall. No PR yet → wait.
- 2026-07-09 tick ~25 · Phase 1 · recovered → PR opened · gates finished (no fix commits ⇒ passed), tree clean at `553a7b44` (all 4 tasks), no procs, no PR, transcript static ~29 min, no completion notification — implementer completed code+gates but went idle before the PR-open step. Orchestrator opened **PR #1497** for the already-pushed, gate-passed branch (258 insertions / 124 deletions across the 6 expected files — within §4 target; implementer≠reviewer preserved, §6a still applies). TaskStop'd the idle agent a1dd… to prevent a duplicate-PR/push when the earlier queued nudge delivers. Next: assess #1497 CI + §6a review.
- 2026-07-09 tick ~26 · Phase 1 · fixer-dispatched · #1497 `commitlint` FAILED = subject-case: commits 007ed1c1 / 67d63eeb / 553a7b44 start with a PascalCase word (AppShell/AppShell/PhoneShell); 652ff5ae is fine. Since the implementer was TaskStop'd, no auto-fix loop runs — dispatched a manual fixer to reword the three subjects lowercase-first (cherry-pick-onto-main + amend, keep DCO + authorship) and force-push. Other blocking checks (build/spectral) were still pending; fixer runs the CI loop to green.
- 2026-07-09 (resume) · Phase 1 · MERGED · PR #1497 squash-merged at 10:26Z. The commitlint fixer (dispatched pre-restart) landed everything in one pass: reworded the 3 PascalCase-first subjects (commitlint green) AND addressed both §6a findings — `79acb31d` restored fillBody flex-delegation in AppShell, `ef1b0eb3` fixed GrillesArchive's stale bottom inset, `b386611f` added fillBody regression tests. Final §6a review marked all 4 findings (2 cycles) resolved, no new findings; sole nit (stale line-count in body) already corrected + §4 override cited (302/−125 = 427, ~7% over soft target). Verified on main: AppShell.tsx + document lock present. Phases 2–5 unblocked. Cron died with prior session; recreating.
- 2026-07-09 (resume) · Phase 2 · dispatched · implementer on `refactor/frontend-grid-app-shell` (Tasks 6–7: migrate PlayScreen + LiveCoopScreen to AppShell overlay, delete their hand-rolled shells; preserve immersive bleed + edge fades + zoom, keep per-screen bottomRef→padBottom coupling). ADR context: ADR-0054 (amended page-shell), ADR-0050 (a11y), ADR-0088 (dark-mode), ADR-0077 (grid credentialed CORS). The delicate phase — flagged extra-wary on grid mechanics.
- 2026-07-09 tick ~ · Phase 2 · PR opened · implementer pushed `refactor/frontend-grid-app-shell` (tip `1f4b347d`) and opened **PR #1499** (`refactor(frontend-ui): grid pages use AppShell overlay`); mergeable MERGEABLE, all CI checks pending (just started). Implementer runs its own auto-fix loop. Decision 3e (CI still running) → wait. Next: assess checks + §6a review as they conclude.
- 2026-07-09 tick ~ · Phase 2 · implementer COMPLETED · PR #1499 (`refactor/frontend-grid-app-shell`, net −49 LOC: 175 ins / 224 del). PlayScreen + LiveCoopScreen onto AppShell overlay; hand-rolled stage/shell css deleted; bottomRef→padBottom coupling kept; SkipLink dedup'd to the shell. Validation: typecheck PASS, vitest 41/41, **shell-scroll-invariants 6/6 on pixel-7 AND iphone-14** (incl. /play). Focused-input caveat CONFIRMED benign (document lock makes iOS focus-scroll unreachable; grid moves by transform). All blocking CI green; only informational claude-review/CodeQL pending → 3b wait for §6a. Decision beyond brief (sound): added an overlay-only `desktopBar` slot to AppShell so the desktop timer + sound toggle aren't silently dropped (grid e2e runs mobile-viewport only) — same mechanism Task 8 gives flow; forward-compatible. SAFETY NOTE: the agent's `git stash pop` during baseline diff accidentally popped a FOREIGN stash (session 1783594811's grilles work); it re-stashed it — verified preserved at `stash@{0}` "foreign-grilles-stash-restored", no data loss, no foreign files in the PR. Reinforces the CLAUDE.md bare-stash prohibition for worktree agents.
- 2026-07-09 tick ~ · Phase 2 · §6a findings (2, both on the `desktopBar` slot) · F1: `desktopBar` wired only into AppShell overlay branch (L107), flow branch (L121) still hardcodes `<DesktopAppBar>` and ignores the prop → PR body's "forward-compatible for Task 8" claim is type-level only; a flow-page passing `desktopBar` would be silently dropped. F2: new slot has no test. Both legit. `claude-review` run still in_progress (~6 min) → **3b wait** (don't race the automated fix cycle). INTENDED FIX DIRECTION if no auto-fix by next tick: keep Phase 2 scoped — (a) soften the PR-body forward-compat claim to note flow-branch wiring is Task 8's job, and (b) add overlay-variant `desktopBar` test coverage per F2. Do NOT pull Task 8's flow-branch wiring into Phase 2 (the plan assigns it to Task 8/Phase 3).
- 2026-07-09 tick ~ · Phase 2 · fixer-dispatched · `claude-review` run stayed in_progress ~15 min with tip frozen at `1f4b347d` and findings posted; per Phase-1 experience this repo's claude-code-review workflow REVIEWS but does not auto-commit fixes (my manual fixer resolved #1497's findings, not the workflow), and the implementer agent has COMPLETED (won't self-fix) → 3c manual fixer. Instructed fix: resolve F1 via option (b) — wire `desktopBar` into the AppShell FLOW branch too (1-line `{desktopBar !== undefined ? desktopBar : <DesktopAppBar active={navActive} />}`, matches plan Task 8 Step 2 "render in both variants"; no flow screen passes desktopBar yet so behavior is unchanged) — plus update the PR body to say the slot is now wired in both variants; resolve F2 by adding app-shell.test.tsx cases (explicit node renders; `null` suppresses; `undefined` falls back to DesktopAppBar). Chose (b) over merely softening the prose because it fully closes the finding and pre-empts the silent-drop bug for Phase 3. Fixer runs gates + CI loop to green.
- 2026-07-09 tick ~ · Phase 2 · fixer pushed `96b25371` · resolves both §6a findings: F1 `AppShell.tsx` (+4/−2) wires `desktopBar` into the flow branch + updates the prop doc; F2 `app-shell.test.tsx` (+34) adds slot coverage. CI re-running, fresh `claude-review` pending → wait for the re-review verdict. Fixer still running its CI loop.
- 2026-07-09 tick ~ · Phase 2 · **MERGED** · PR #1499 squash-merged at 11:27:15Z. Fresh §6a re-review (cycle 2) = "LGTM, no findings" — both desktopBar findings resolved (F1 wired into overlay+flow both, F2 test coverage added). All blocking checks green; only informational CodeQL pending (non-blocking). PlayScreen + LiveCoopScreen now on AppShell overlay; hand-rolled grid shells deleted; immersive bleed/edge-fades/zoom preserved; shell-scroll-invariants 6/6 on pixel-7 + iphone-14. **Phase 3 (HomeScreen → AppShell flow, Task 8, branch `refactor/frontend-home-app-shell`) unblocked — dispatches next tick.** Phases 4–5 pending.
- 2026-07-09 tick ~ · Phase 3 · dispatched · implementer on `refactor/frontend-home-app-shell` (Task 8: HomeScreen → AppShell flow). SCOPE REDUCED: Task 8 Step 2 (add `desktopBar` slot to AppShell + its test) is ALREADY on main via the Phase-2 fixer (PR #1499) — verified AppShell props on main carry `desktopBar` (rendered both variants, `!== undefined` semantics, L107/L121), `headerFlush`, `topBar`/`bottomBar`/`navActive`/`fillBody`; app-shell.test.tsx already covers the slot. So Phase 3 = Steps 1,3,4,5,6 only: swap HomeScreen's hand-rolled shell/frame/content for `<AppShell variant="flow" navActive="accueil" headerFlush topBar={MobileTopBar} bottomBar={miniGameTyping? undefined : BottomNav} desktopBar={<DesktopAppBar active="accueil" streak={streak.cur}/>}>`, delete the dead shell/frame/content css, drop the now-unused SkipLink import, preserve the mini-game-typing bottom-nav-hide branch. ADR context: adr-context.sh empty → named ADR-0054 (amended), ADR-0050 (a11y), ADR-0002. Told the implementer to read AppShell.tsx on main for the exact prop names rather than trust the plan snippet, and NOT to re-add the desktopBar slot.
- 2026-07-09 · Phase 2 · fixer aac3481 completed (post-merge, benign race) · The fixer's first push `96b25371` was the commit I merged into #1499 (squash → `4fa37645` on main); it then kept running unaware, re-authored functionally-identical changes, and on its final push RE-CREATED the merged head branch `refactor/frontend-grid-app-shell` as a dangling ref at `00811889` (redundant post-merge commit, no open PR, content already in main via the squash). Verified merged main is correct: AppShell.tsx desktopBar wired in BOTH variants (L107 overlay + L121 flow), doc comment updated, app-shell.test.tsx has the `describe.each(['flow','overlay'])` slot coverage (3 contract cases). CLEANUP BLOCKED: `git push origin --delete refactor/frontend-grid-app-shell` was auto-denied (destructive-git, not user-named) — left for the maintainer; harmless, the phase walk keys on PR merged-state not branch existence so it won't cause re-dispatch. NOTE: the fixer also reported local `pnpm test` shows 36 pre-existing failures across 13 files IDENTICAL on the base tip (root causes: local node 22.22.0 < required 22.22.3, + preview-env missing mock data) — zero regressions from the desktopBar work; flagged so future phases don't misread these as new breakage.
- 2026-07-09 · Phase 3 · liveness nudge · implementer `a7fa149c` transcript flat ~4 min (13:42:39→13:46:21) with no live test procs, no branch/PR yet — third consecutive flat window. Queued a SendMessage liveness check (report status, or finish: gates → commit lowercase-first → push → open PR; reminded NOT to touch AppShell.tsx/app-shell.test.tsx since the desktopBar slot is already on main). Benign if it's mid model-turn (per the tick-19/20 lesson, transcript-flat alone isn't a stall). NOT escalating — no failure signal, work not yet started to push. Reassess next tick.
- 2026-07-09 · Phase 3 · WEDGED → recovered · implementer `a7fa149c` transcript froze at 13:42:39 for ~5m40s (through 13:48:21) with ZERO tool rounds — the queued liveness nudge never delivered (no next tool round), no test procs, no branch/PR. Against its prior cadence (activity every few–30s) this is a genuine hang, not a long model-turn. TaskStop'd it (nothing pushed → zero loss) and re-dispatching a fresh Phase 3 implementer on the same branch `refactor/frontend-home-app-shell` with the identical scoped brief (HomeScreen → AppShell flow; do NOT touch AppShell.tsx/app-shell.test.tsx — desktopBar slot already on main). Distinct from the tick-19/20 misdiagnosis: there the transcript WAS advancing + live test procs existed; here transcript is fully frozen with an undelivered nudge.
- 2026-07-09 · Phase 3 · recovered → PR #1500 (NO re-dispatch) · The wedged agent's kill notification revealed it had actually completed the HomeScreen migration AND solved a desktop-width regression (hub squeezed by AppShell's 680 reading column) before freezing right before `pnpm a11y`. Found its uncommitted work intact in worktree `refactor-home-app-shell`: HomeScreen.tsx only (+26/−75), correctly scoped (AppShell.tsx untouched). Reviewed the diff — clean & complete: swaps hand-rolled shell/frame/content for `<AppShell variant="flow" navActive="accueil" headerFlush topBar bottomBar={miniGameTyping?undefined:BottomNav} desktopBar={DesktopAppBar streak}>`, deletes dead css, drops SkipLink import + the `lang="fr"` attr (verified redundant — AppShell root sets `lang="fr"` in both variants + index.html has `<html lang="fr">`, so NOT a regression), and adds an `lg`-only `hubBleed` wrapper (100vw bleed via marginLeft:50%+translateX(-50%); `display:contents` passthrough on phone/tablet) so the 1140-capped 2-col hub escapes the flow reading column. Rather than re-dispatch (would redo good work + lose the hubBleed insight), finished it: committed `56de008f` (my authorship + DCO; wedged agent's design preserved), pushed, opened **PR #1500** (net −49). Same recovery pattern as Phase-1 #1497. CI + §6a validate now (the a11y/e2e/tests the wedged agent never ran happen in CI); dispatch a fixer if anything fails. Supersedes the prior "re-dispatching fresh implementer" line.
- 2026-07-09 · Phase 3 · **MERGED** · PR #1500 squash-merged at 11:58:02Z. §6a = "LGTM, no findings"; all blocking checks green. Only failing check was `Analyze (java-kotlin)` = CodeQL, INFORMATIONAL (never blocks) and impossible to be caused by a TS-only HomeScreen change — noted, not gating. HomeScreen now on AppShell flow with the `hubBleed` desktop-width fix. **Phase 4 (routes/ + v2/multiplayer/SalonScreen, Task 9 part 1, branch `refactor/frontend-appshell-routes`) unblocked — dispatches next tick.** Phase 5 (v2/* + delete PhoneShell) depends on Phase 4.
- 2026-07-09 · Phase 4 · dispatched · implementer on `refactor/frontend-appshell-routes` (Task 9 part 1: routes/ + SalonScreen only). Scope = 5 files, all confirmed on main to use PhoneShell (play.tsx:7, join.$code.tsx:7, lobby.$lobbyId.tsx:9, __root.tsx:1, v2/multiplayer/SalonScreen.tsx:3), NONE use fillBody. Explicitly EXCLUDED: the v2/* screens and the PhoneShell.tsx deletion — those are Phase 5 (PhoneShell must be deleted LAST, after all call sites migrate). STALENESS WARNING passed to implementer: the plan recipe says "drop fillBody (no-op)" but Phase 1's §6a Finding restored fillBody as a REAL flex-delegation prop — none of these 5 files use it so it's moot, but if seen, preserve don't drop. Told it to read PhoneShell.tsx (thin wrapper over AppShell since Phase 1) to confirm the header→topBar / bottomNav→bottomBar prop mapping. ADR context: adr-context.sh empty → named ADR-0054 (amended), ADR-0050 (a11y), ADR-0002. Note: a stray `git checkout origin/main -- .` during scoping polluted the docs worktree with 42 files; reset --hard HEAD + clean, verified clean before dispatch.
- 2026-07-09 · Phase 4 · CORRECTION (§6a review, PR #1492) · Two factual errors in the tick-54 dispatch line, both confirmed independently against the actual files/registry rather than the log's claims. (1) `__root.tsx` does NOT use PhoneShell — no PhoneShell import; `RootErrorBoundary`/`RootNotFound` render bare `<main>`, and the file's own comment (L114) says they deliberately skip PhoneShell since the route tree itself may be broken. Real Phase 4 scope is 4 files, not 5 (play.tsx, join.$code.tsx, lobby.$lobbyId.tsx, v2/multiplayer/SalonScreen.tsx); `__root.tsx` takes no migration action, it was never a shell call site. (2) `adr-context.sh` would NOT return empty on the real 5-path set — INDEX.md rows ADR-0074 (`frontend/src/ui/routes/**`, line 162), ADR-0088 (`frontend/src/ui/v2/**`, line 232), and ADR-0098 (`frontend/src/ui/routes/lobby.$lobbyId.tsx`, exact-file, line 290, 2026-07-08 `handleClaim` amendment) all glob-match, alongside ADR-0054 which was already named. ADR-0098 is the load-bearing miss: its `handleClaim` logic (`lobby.$lobbyId.tsx:173`) sits in the same file as three PhoneShell call sites (lines 40, 50, 65, 248) — the L248 site renders the claim-flow UI directly. Implementer must read ADR-0098 before touching that file's PhoneShell→AppShell swap. The tick-54 implementer was already dispatched on the uncorrected brief; next tick confirms it read the corrected scope/ADR set (or reads this correction) before merge, else a fixer re-briefs it.
- 2026-07-09 · Phase 4 · implementer COMPLETED, PR #1501 opened cleanly · `refactor/frontend-appshell-routes` tip `2b0dc8eb`. Diff is EXACTLY the 5 scoped files, pure mechanical rename (27 ins / 27 del): routes/{__root,join.$code,lobby.$lobbyId,play}.tsx + v2/multiplayer/SalonScreen.tsx. Verified scope: `git grep PhoneShell` on the branch shows references remain ONLY in the Phase-5 v2/* screens (APropos, Compte, GrillesArchive, Reglages, …) + PhoneShell.tsx itself — routes/ and SalonScreen fully migrated, PhoneShell.tsx untouched (correct: it dies in Phase 5). Agent's final msg was "waiting for monitor events" so its CI auto-fix loop is now dead (agent stopped) — orchestrator owns #1501's CI + §6a from here. Assess next tick.
- 2026-07-09 · Phase 4 · CORRECTION #2 (§6a review, PR #1492) · The prior correction's own ADR-0098 pointer was itself inaccurate — same failure mode it was fixing, caught by re-verifying directly against `frontend/src/ui/routes/lobby.$lobbyId.tsx` on `origin/main` rather than trusting the count/description. There are **four** PhoneShell call sites, not three: lines 41, 51, 66, 255. And L255 (the site nearest the prior correction's "L248") wraps the **COMPLETED**-state branch (`ResultatsScreen` + `OwnedGameModal` — the post-game replay/rejoin UI, `onReplay`/`onHome`/`coop.rejoindre`/`coop.startNewGame`), not the claim flow. `handleClaim` (defined L179–188) is instead wired as `onClaim={handleClaim}` into `LiveCoopScreen` at L243, inside the **IN_PROGRESS** branch (L225–247) — which renders bare in a `<>` fragment with **no PhoneShell wrapper at all** (that screen moved to AppShell overlay in Phase 2, PR #1499). The caution stands — read ADR-0098 before touching this file's PhoneShell→AppShell swaps — but the risk sits specifically at the `handleClaim` callback (L179–188) and its `onClaim` wiring (L243), neither of which is inside any of the file's four PhoneShell sites (41, 51, 66, 255). Don't disturb `handleClaim`/`onClaim` while migrating the four unrelated PhoneShell sites.
- 2026-07-09 · Phase 4 · **MERGED** · PR #1501 squash-merged at 12:21:04Z. §6a = "LGTM, no findings" (verified the 1:1 header→topBar / variant=flow prop mapping against AppShell.tsx/PhoneShell.tsx). All blocking checks green; only informational Analyze/deploy pending. routes/{__root,join,lobby,play}.tsx + v2/multiplayer/SalonScreen.tsx now on AppShell. **Phase 5 (FINAL: migrate remaining v2/* screens + `git rm PhoneShell.tsx`, branch `refactor/frontend-appshell-v2`) unblocked — dispatches next tick.** After Phase 5 merges: PhoneShell is gone, AppShell is the sole shell, rollout complete → maintainer's on-device PWA verification pass is the last gate.
- 2026-07-09 · Phase 5 (FINAL) · dispatched · implementer on `refactor/frontend-appshell-v2` (Task 9 part 2 + PhoneShell deletion). Scope = the 15 remaining v2/* screens still on PhoneShell (APropos, AbonnementAnnule/Abonnement/AbonnementSucces, Aide, Compte, ConditionsAbonnement, Confidentialite, Connexion, GateLoading, GrillesArchive, LoaderRetry, MentionsLegales, NotFound, Reglages) → AppShell flow via the same recipe, THEN `git rm frontend/src/ui/v2/PhoneShell.tsx` once `grep -rn PhoneShell frontend/src` is empty. CRITICAL per-screen warnings passed: (1) GrillesArchiveScreen.tsx uses `fillBody` (verified on main) — PRESERVE it, do NOT drop; it drives the pinned tab-head (was §6a Finding 1 on #1497). The plan's "drop fillBody (no-op)" note is STALE. (2) PhoneShell.tsx has fillBody×3 in its own passthrough but it's being deleted. Deletion must be LAST and only after the grep is clean. Diff = 15 small renames + one file delete, expected under the 400 cap (all in v2/, one PR). ADR context: adr-context.sh empty → named ADR-0054 (amended), ADR-0050 (a11y), ADR-0002. After this merges: PhoneShell gone, AppShell sole shell, rollout complete.
- 2026-07-09 · Phase 4 · CORRECTION #3 (§6a review, PR #1492) · Correction #2's line numbers were themselves off (reasoned rather than directly checked, the same defect class for a third time on this file) — the count (four) and qualitative claims (L248 site = COMPLETED-branch replay/rejoin UI, not claim flow; `handleClaim`/`onClaim` sit in the PhoneShell-free IN_PROGRESS branch) were right, only the numbers were wrong. Verified this time via `git show b8710793:frontend/src/ui/routes/lobby.\$lobbyId.tsx | grep -n` (`b8710793` = `origin/main` immediately prior to PR #1501's Phase-4 migration, now merged per the tick above) rather than reasoning from memory: PhoneShell opening tags at **lines 40, 50, 65, 248** (not 41/51/66/255); `handleClaim` defined **L173–183** (not L179–188); `onClaim={handleClaim}` wiring at **L236** (not L243); IN_PROGRESS branch spans **L218–243** (not L225–247); COMPLETED branch starts **L246**. Net effect on the guidance is unchanged — `handleClaim`/`onClaim` still sit outside all four PhoneShell sites, in the wrapper-free IN_PROGRESS branch — only the exact line references move. This file has now needed three correction passes on the same claim; per CLAUDE.md "three patches on the same path = the shape is wrong," the fix here is procedural, not another patch: these line numbers describe **historical, pre-migration** state only (superseded now that PR #1501 merged AppShell into this file) — any future reference to this file's shell markup must re-verify against its current content, not against this entry.
- 2026-07-09 · Phase 4 · CORRECTION #3 RETRACTED, thread closed (§6a review, PR #1492) · Correction #3 verified against the wrong base commit: `b8710793` is not `origin/main` immediately prior to PR #1501 — it is several commits further back, and 17 commits (including `c82f3195`, `#1488`, which itself touches this file, +11/−4) separate it from `daba199e`, PR #1501's squash-merge's actual parent (confirmed via `git cat-file -p 95b47f6b`). Re-verified directly against `daba199e` (`git grep -n 'PhoneShell\|handleClaim\|onClaim=' daba199e -- frontend/src/ui/routes/lobby.\$lobbyId.tsx` + `git show daba199e:...`): PhoneShell opening tags at **L41, L51, L66, L255**; `handleClaim` defined at **L180**; `onClaim={handleClaim}` wiring at **L243**; IN_PROGRESS branch at **L225**; COMPLETED branch at **L253** — this matches Correction #2, not Correction #3; Correction #2's numbers were the accurate ones all along. Per CLAUDE.md "three patches on the same path = the shape is wrong," this is the fourth correction pass on the same claim and the underlying fix is procedural: PhoneShell→AppShell migration for `lobby.$lobbyId.tsx` is done (merged in PR #1501), Phase 5's scope is the `v2/*` screens + `PhoneShell.tsx` deletion only (see the two ticks above) and does not touch this file again, so this log has no further reason to track its shell-markup line numbers. No more corrections should be appended for this file — any future reader needing its shell markup should read the file on `main` directly, not this log.
- 2026-07-09 · Phase 5 (FINAL) · PR #1502 opened · `refactor/frontend-appshell-v2` tip `8e05c7e4`. Diff = 16 files (15 v2/* screens migrated + PhoneShell.tsx DELETED), 50 ins / 84 del. Verified: `git grep PhoneShell frontend/src` on the branch = NONE (clean), PhoneShell.tsx gone. Agent's pre-push gates (build + e2e chromium + a11y + pixel-7 shell-scroll-invariants) ran ~7 min in-worktree and passed. CI + §6a assessing now → decision 3e/3b wait. On merge: AppShell is the sole shell, PhoneShell retired, the whole rollout (Phases 0–5) is complete — only the maintainer's on-device PWA verification remains.
- 2026-07-09 · Phase 5 · commitlint fix (orchestrator inline) · #1502's `commitlint` FAILED = body-max-line-length + footer-max-line-length: both of the implementer's commit bodies were single lines >100 chars (subjects were fine). Since the implementer was TaskStop'd (no auto-fix loop), and this is a pure message reword (no code change), fixed it inline: in a throwaway worktree off the branch, `git reset --soft origin/main` + one `git commit -s` squashing both commits with a body wrapped at ≤100 chars (longest line now 79), then `git push --force-with-lease`. Verified the new tree (2d082572) is IDENTICAL to the old tip 8e05c7e4 (empty diff) — message-only change, zero code delta. CI re-runs; assess next tick.
- 2026-07-09 · Phase 5 · §6a Finding 1 fixed (orchestrator inline) · First-cycle review: LGTM on all substance (prop mapping 1:1, GrillesArchive fillBody untouched, shim truly gone, diff 134 lines under cap) EXCEPT one completeness finding — 4 test files still had comments naming the deleted PhoneShell (`abonnement-annule-route`, `abonnement-consent`, `abonnement-route`, `abonnement-succes-route`), for screens THIS PR migrates. Root cause: the implementer's grep covered `frontend/src` only, not `frontend/tests`. Reviewer explicitly scoped out `v2-resultats`/`v2-salon` (migrated in earlier phases, pre-existing staleness). Implementer stopped → fixed inline: sed'd the 4 comments PhoneShell→AppShell (accurate — AppShell owns the same router/DesktopAppBar/MenuSheet wiring), committed `243f76bf` (`test(frontend-ui): ...`, body ≤100), normal push. CI + §6a re-run; assess next tick.
- 2026-07-09 · Phase 5 (FINAL) · **MERGED** · PR #1502 squash-merged at 13:18:57Z. §6a cycle-2 = "LGTM, no findings" (Finding 1 resolved by the `test(...)` comment fix); mergeStateStatus CLEAN, ALL checks green (incl. CodeQL this run). Verified on main: `PhoneShell.tsx` DELETED, `git grep PhoneShell frontend/src` = empty. **AppShell is now the sole shell.**
- 2026-07-09 · **ROLLOUT COMPLETE** · All 6 phases merged: P0 ADR-0054 amend (#1493/#1495) · P1 document lock + AppShell primitive (#1497) · P2 grid pages overlay (#1499) · P3 HomeScreen flow + hubBleed (#1500) · P4 routes+SalonScreen (#1501) · P5 v2/* screens + PhoneShell deletion (#1502). The root-cause fix (html/body/#root overflow lock in index.css) has been on main since P1; the phantom-scrollbar bug is addressed but NOT yet verified — the target bug is not CI-reproducible, so the maintainer's on-device PWA pass is the sole functional gate and the ONLY remaining step. Cron `CronDelete`'d — no further ticks. Holding-branch docs PR #1492 left OPEN for the maintainer (it carries this full spec/plan/procedure/log narrative; merging it lands the record on main).
