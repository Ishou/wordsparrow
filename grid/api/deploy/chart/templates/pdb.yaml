{{- if gt (int (.Values.replicaCount | default 2)) 1 }}
{{/* ADR-0101 R5: keep >=1 pod during voluntary disruptions (node drain/upgrade). Gated on >1 replica so single-replica local doesn't block drains. */}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "wordsparrow-api.fullname" . }}
  labels: {{- include "wordsparrow-api.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels: {{- include "wordsparrow-api.selectorLabels" . | nindent 6 }}
{{- end }}
