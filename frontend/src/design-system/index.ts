// WordSparrow design system v2 (ADR-0072) — standalone module, isolated from
// the app via eslint-boundaries until the migration. Components land in later
// waves; this barrel is their single public entry point.
export const DESIGN_SYSTEM_VERSION = '2.0.0-alpha';
