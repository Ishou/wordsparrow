# Default Grid API base URL — production. Per ADR-0007 §5 preview
# environments override this to point at the spec-driven MSW worker.
VITE_GRID_API_URL=https://api.wordsparrow.io

# Default Game API base URL — production. Both the REST surface
# (HttpLobbyClient) and the WebSocket surface (WebSocketGameClient)
# derive their URLs from this single value. The composition root
# in `main.tsx` reads it and threads adapters through router context.
VITE_GAME_API_BASE_URL=https://game.wordsparrow.io

# Multiplayer feature flag (ADR-0018 §10). Enabled in production now
# that the game-api Helm chart is live; also enabled in
# `.env.development` and `.env.preview`, both of which run against
# MSW so the lobby REST + WebSocket surfaces work without a backend.
# Expires 2026-08-02 (90 days from first enablement).
VITE_FEATURE_MULTIPLAYER=true

# Passwordless email-OTP flag (ADR-0091). Ships dark: OFF in production
# until the identity backend flag flips bright. `.env.preview` enables
# it so the /connexion flow is reviewable against MSW. Flag retirement:
# 2026-10-01.
VITE_FEATURE_EMAIL_AUTH=false

# MSW handlers, per surface. Both off in production — the real
# backends serve every request. `.env.preview` flips both on so the
# Cloudflare Pages preview is self-contained; `.env.development`
# flips only `VITE_MOCK_GAME_API` on so `pnpm dev` runs against a
# real grid backend on localhost while still demoing multiplayer
# without the game-api Helm chart running.
VITE_MOCK_GRID_API=false
VITE_MOCK_GAME_API=false

# Self-hosted Matomo (ADR-0025). When both vars are set, the cookieless
# tracker loads matomo.js and reports page views + custom events.
# `.env.development` and `.env.preview` blank these so local dev and
# preview builds never reach the prod analytics server.
VITE_MATOMO_URL=https://analytics.wordsparrow.io
VITE_MATOMO_SITE_ID=1

# OpenTelemetry browser SDK (ADR-0033 / PR-F.2). When the endpoint is
# set, the SDK auto-instruments fetch + initial document load and POSTs
# OTLP/HTTP traces to the public ingest. Empty in
# `.env.development` / `.env.preview` so local + preview never publish.
# Sampler ratio 0.1 = 10% of traces; raise via VITE_OTEL_SAMPLER_RATIO
# once we have real volume to tune from.
VITE_OTEL_OTLP_ENDPOINT=https://otlp.wordsparrow.io
VITE_OTEL_SAMPLER_RATIO=0.1
VITE_OTEL_SERVICE_NAME=frontend
