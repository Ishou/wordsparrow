name: Deploy API (k8s)

# Implements ADR-0009 §8 step 5: continuous delivery of the WordSparrow
# services to the Hetzner k3s cluster (FSN1, 1 CP + 1 worker, ADR-0009 §2).
#
# The workflow is a matrix that fans out over the {grid, game, identity,
# survey, billing} bounded contexts:
#   - wordsparrow-api          → grid/api/deploy/chart,     api.wordsparrow.io
#   - wordsparrow-game-api     → game/api/deploy/chart,     game.wordsparrow.io
#   - wordsparrow-identity-api → identity/api/deploy/chart, auth.wordsparrow.io
#   - wordsparrow-survey-api   → survey/api/deploy/chart,   survey.wordsparrow.io
#   - wordsparrow-billing-api  → billing/api/deploy/chart,  billing.wordsparrow.io
#
# Triggered by `Build and Push Image` (build-and-push-image.yml) on every
# successful main push: each matrix row of the build workflow uploads
# `image-digest-<context>` as an artifact; this workflow downloads the
# matching one and runs `helm upgrade --install <release-name> <chart-path>`
# with `--set image.digest=<sha256>` so each chart's `image.requireDigest`
# guard is satisfied. `workflow_dispatch` is wired for manual rollback /
# redeploy: pass the bounded context + desired digest as inputs.
#
# Runs ALONGSIDE the existing Fly deploy at .github/workflows/deploy-api.yml
# until ADR-0009 §8 step 7 cutover removes Fly.
#
# Images are immutable per the MANIFESTO ("Container images pinned to
# digest. Builds are deterministic."): no `:latest`, no floating tags
# reach the cluster. The chart fails fast (helm `fail`) when no digest
# is supplied, so a degraded artifact path here surfaces as a build
# failure, not a silent untagged deploy.
#
# Failure handling: `--atomic` was dropped on purpose so diagnostics can
# capture the broken pods/events before rollback. The
# "Capture failure diagnostics" + "Rollback failed release" steps
# replicate `--atomic`'s rollback semantics while keeping the manifesto's
# "one-click rollback" promise intact.
#
# PREREQUISITES (one-time human bootstrap, see docs/deploy.md):
#   - The Hetzner k8s cluster + platform operators (cert-manager,
#     ingress-nginx, external-dns, CNPG, hcloud-csi) are bootstrapped
#     via `infra/platform/` per ADR-0009 §3 (PR56).
#   - The `KUBECONFIG_PROD` repository secret holds the cluster's
#     kubeconfig with the rewritten public IP.
#   - Per-context env secrets exist in the `wordsparrow` namespace:
#       - `wordsparrow-api-env` (grid)            (ADR-0009 §10)
#       - `bliss-game-api-env` (game)             (ADR-0018, see chart values)
#       - `bliss-identity-api-env` (identity)     (ADR-0045; carries OIDC client
#                                                  creds + COOKIE_DOMAIN +
#                                                  ALLOWED_RETURN_ORIGINS;
#                                                  bootstrap recipe in PR 4.5e)
#       - `bliss-billing-api-env` (billing)       (ADR-0078; carries MOLLIE_API_KEY, test_… during the test phase)
#   - The GHCR packages are PUBLIC (one-time visibility flip per
#     build-and-push-image.yml's header):
#       - bliss/wordsparrow-api          (already public)
#       - bliss/wordsparrow-game-api     (already public)
#       - bliss/wordsparrow-identity-api (NEW — must be flipped after first push)
#       - bliss/wordsparrow-billing-api  (NEW — must be flipped after first push)

on:
  workflow_run:
    # Must match the upstream workflow's `name:` field exactly.
    workflows: ["Build and Push Image"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      context:
        description: 'Bounded context to deploy.'
        required: true
        type: choice
        options:
          - grid
          - game
          - identity
          - survey
          - billing
        default: grid
      digest:
        description: 'Image digest to deploy, format "sha256:..." (manual rollback / redeploy).'
        required: true
        type: string
      worker-digest:
        description: ':grid:worker image digest, format "sha256:..." — required when context=grid (ensure-dailies CronJob, ADR-0042). Ignored otherwise.'
        required: false
        type: string
        default: ""

concurrency:
  # Serialize prod deploys: a second push mid-deploy queues rather than
  # cancels (cancel-in-progress: false). Mid-flight `helm upgrade`
  # cancellation can leave the release in a `pending-upgrade` state
  # that requires manual `helm rollback`, which violates the one-click
  # rollback rule. The matrix runs grid + game in parallel within a
  # single workflow run; the concurrency group serializes runs against
  # each other, not jobs within one run.
  group: deploy-api-k8s-prod
  cancel-in-progress: false

permissions:
  # Least-privilege per CLAUDE.md "Security". `actions: read` is
  # required for actions/download-artifact's cross-workflow form
  # (downloading from another workflow run via `run-id`).
  contents: read
  actions: read

jobs:
  deploy:
    name: deploy (${{ matrix.context }})
    # workflow_run path: only proceed on successful upstream push builds.
    # workflow_dispatch path: every matrix row is launched, but rows whose
    # context doesn't match `inputs.context` short-circuit at the first
    # step (see "Filter dispatch target") so a manual rollback to one
    # service doesn't accidentally redeploy the other with the same
    # digest (which would also be wrong — digests are distinct).
    #
    # NOTE: `matrix.*` is NOT available in a job-level `if:` because GHA
    # evaluates job-level conditions before matrix expansion. That's why
    # the per-context dispatch filter lives in a step, not here.
    # See https://github.com/actions/runner/issues/1985.
    if: >-
      ${{ github.event_name == 'workflow_dispatch' ||
          (github.event_name == 'workflow_run' &&
           github.event.workflow_run.conclusion == 'success' &&
           github.event.workflow_run.event == 'push') }}
    runs-on: ubuntu-latest
    # 30 m covers the cold-bootstrap path: checkout + helm setup +
    # artifact + kubeconfig (~4 m) + db-chart install (~1 m) + kubectl
    # wait for CNPG primary election + replica join on Hetzner volumes
    # (up to 15 m, see Wait step) + api-chart install (~5 m) + margin.
    # Steady-state runs use only a fraction of this budget.
    timeout-minutes: 30
    strategy:
      # fail-fast: false so a green grid deploy isn't masked by a red
      # game deploy (and vice versa). Matches build-and-push-image.yml.
      fail-fast: false
      matrix:
        include:
          - context: grid
            release-name: wordsparrow-api
            chart-path: grid/api/deploy/chart/
            values-file: grid/api/deploy/chart/values-prod.yaml
            digest-artifact: image-digest-grid
            # Grid chart also pins the :grid:worker image used by the
            # ensure-dailies CronJob (ADR-0042); empty for contexts that
            # don't ship a worker so the download/resolve steps no-op.
            worker-digest-artifact: image-digest-grid-worker
            health-url: https://api.wordsparrow.io/v1/health
            # grid's CNPG cluster is still owned by its api chart;
            # the split mirrors game-api but is a follow-up PR. Empty
            # values skip the db-chart steps for this row.
            db-release-name: ""
            db-chart-path: ""
            db-values-file: ""
          - context: game
            release-name: wordsparrow-game-api
            chart-path: game/api/deploy/chart/
            values-file: game/api/deploy/chart/values-prod.yaml
            digest-artifact: image-digest-game
            worker-digest-artifact: ""
            health-url: https://game.wordsparrow.io/v1/health
            # CNPG Cluster lifecycle is split out of the api chart so a
            # failed api deploy's auto-rollback can never destroy the
            # cluster mid-bootstrap (regression observed 2026-05-12,
            # ADR-0055 amendment). The db chart is install-once-update-
            # rarely; idempotent on every workflow run. The release name
            # matches the wordsparrow-* convention so the CNPG-managed
            # Secret/Service (`<release>-app`, `<release>-rw`) line up
            # with the api chart's `database.clusterName`.
            db-release-name: wordsparrow-game-api-pg
            db-chart-path: game/api/deploy/db-chart/
            db-values-file: game/api/deploy/db-chart/values-prod.yaml
          # identity-api (ADR-0044, PR #500). Same chart-split pattern as
          # game-api. Pre-merge gates: PR 4.5e bootstraps the
          # `bliss-identity-api-env` Secret and flips the GHCR package
          # visibility — until then, the first identity-context run on
          # main may fail at "wait-for-postgres" (Secret missing) or
          # ImagePullBackOff (package private). Both are recoverable
          # by completing PR 4.5e's manual steps.
          - context: identity
            release-name: wordsparrow-identity-api
            chart-path: identity/api/deploy/chart/
            values-file: identity/api/deploy/chart/values-prod.yaml
            digest-artifact: image-digest-identity
            worker-digest-artifact: ""
            health-url: https://auth.wordsparrow.io/v1/health
            db-release-name: wordsparrow-identity-api-pg
            db-chart-path: identity/api/deploy/db-chart/
            db-values-file: identity/api/deploy/db-chart/values-prod.yaml
          # survey-api (ADR-0056). Same chart-split pattern as game-api +
          # identity-api. The api image bundles the :survey:worker installDist
          # so the chart's post-install seed Job and hourly retire CronJob
          # share the same digest (no separate worker artifact).
          # Pre-merge gates: the bootstrap of `bliss-survey-api-env` Secret
          # and the GHCR package visibility flip — until both are done, the
          # first survey-context run on main may fail at wait-for-postgres
          # (Secret missing) or ImagePullBackOff (package private). Both
          # recoverable by completing the matching docs/deploy.md steps.
          - context: survey
            release-name: wordsparrow-survey-api
            chart-path: survey/api/deploy/chart/
            values-file: survey/api/deploy/chart/values-prod.yaml
            digest-artifact: image-digest-survey
            worker-digest-artifact: ""
            health-url: https://survey.wordsparrow.io/v1/health
            db-release-name: wordsparrow-survey-api-pg
            db-chart-path: survey/api/deploy/db-chart/
            db-values-file: survey/api/deploy/db-chart/values-prod.yaml
          # billing-api (ADR-0078); chart-split mirrors identity. Pre-merge gates: bootstrap `bliss-billing-api-env` (MOLLIE_API_KEY) + flip GHCR visibility, else first run fails at wait-for-postgres or ImagePullBackOff (docs/deploy.md).
          - context: billing
            release-name: wordsparrow-billing-api
            chart-path: billing/api/deploy/chart/
            values-file: billing/api/deploy/chart/values-prod.yaml
            digest-artifact: image-digest-billing
            worker-digest-artifact: ""
            health-url: https://billing.wordsparrow.io/v1/health
            db-release-name: wordsparrow-billing-api-pg
            db-chart-path: billing/api/deploy/db-chart/
            db-values-file: billing/api/deploy/db-chart/values-prod.yaml
    environment:
      # Per-context environment so each appears separately in the GitHub
      # Environments UI and required-reviewer rules can be applied
      # independently (e.g. only require approval for grid deploys).
      name: prod-${{ matrix.context }}
      url: ${{ matrix.health-url }}

    steps:
      - name: Filter dispatch target
        # On workflow_dispatch, only the matrix row whose context matches
        # `inputs.context` should actually deploy; the other row no-ops.
        # On workflow_run, every row runs (both contexts deploy after a
        # successful main build). Sets RUN_ROW=true|false in $GITHUB_ENV;
        # every subsequent step gates on `env.RUN_ROW == 'true'`.
        # billing auto-deploy is gated behind vars.BILLING_DEPLOY_ENABLED until its prerequisites (MOLLIE_API_KEY secret + GHCR access) exist — ADR-0078 ship-dark.
        env:
          EVENT_NAME: ${{ github.event_name }}
          DISPATCH_CONTEXT: ${{ inputs.context }}
          ROW_CONTEXT: ${{ matrix.context }}
          BILLING_DEPLOY_ENABLED: ${{ vars.BILLING_DEPLOY_ENABLED }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "workflow_dispatch" ] \
             && [ "$DISPATCH_CONTEXT" != "$ROW_CONTEXT" ]; then
            echo "Dispatch target is '$DISPATCH_CONTEXT'; skipping '$ROW_CONTEXT' row."
            echo "RUN_ROW=false" >> "$GITHUB_ENV"
          elif [ "$EVENT_NAME" != "workflow_dispatch" ] \
             && [ "$ROW_CONTEXT" = "billing" ] \
             && [ "${BILLING_DEPLOY_ENABLED:-}" != "true" ]; then
            echo "billing auto-deploy gated off (set repo var BILLING_DEPLOY_ENABLED=true once the MOLLIE_API_KEY secret + GHCR access are in place — ADR-0078 ship-dark)."
            echo "RUN_ROW=false" >> "$GITHUB_ENV"
          else
            echo "RUN_ROW=true" >> "$GITHUB_ENV"
          fi

      - name: Checkout repository
        if: env.RUN_ROW == 'true'
        # actions/checkout v4.3.1 — same SHA as helm-lint.yml.
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 1

      - name: Set up Helm
        if: env.RUN_ROW == 'true'
        # azure/setup-helm v4.3.1 — same SHA as helm-lint.yml.
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1
        with:
          version: v4.2.1

      - name: Download digest artifact (workflow_run)
        # Cross-workflow artifact download: the build workflow uploaded
        # `image-digest-<context>` in the run identified by `workflow_run.id`.
        if: env.RUN_ROW == 'true' && github.event_name == 'workflow_run'
        # actions/download-artifact v4.3.0
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ matrix.digest-artifact }}
          path: artifact
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve image digest
        if: env.RUN_ROW == 'true'
        id: digest
        env:
          DISPATCH_DIGEST: ${{ inputs.digest }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            digest="$DISPATCH_DIGEST"
          else
            digest="$(cat artifact/digest.txt)"
          fi
          if [ -z "$digest" ]; then
            echo "::error::resolved digest is empty"
            exit 1
          fi
          case "$digest" in
            sha256:*) ;;
            *)
              echo "::error::digest must start with 'sha256:'; got '$digest'"
              exit 1
              ;;
          esac
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "Resolved digest: $digest"

      - name: Download worker digest artifact (workflow_run)
        # ADR-0042: contexts that ship a :grid:worker pin its image
        # alongside the api image. Same cross-workflow handoff as the
        # main digest above. No-op for rows whose worker-digest-artifact
        # is empty (e.g. game).
        if: env.RUN_ROW == 'true' && github.event_name == 'workflow_run' && matrix.worker-digest-artifact != ''
        # actions/download-artifact v4.3.0 — same SHA as above.
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ matrix.worker-digest-artifact }}
          path: worker-artifact
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve worker image digest
        if: env.RUN_ROW == 'true' && matrix.worker-digest-artifact != ''
        id: worker-digest
        env:
          DISPATCH_WORKER_DIGEST: ${{ inputs.worker-digest }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            digest="$DISPATCH_WORKER_DIGEST"
          else
            digest="$(cat worker-artifact/digest.txt)"
          fi
          if [ -z "$digest" ]; then
            echo "::error::worker digest is empty — pass 'worker-digest' input on dispatch (required for grid)"
            exit 1
          fi
          case "$digest" in
            sha256:*) ;;
            *)
              echo "::error::worker digest must start with 'sha256:'; got '$digest'"
              exit 1
              ;;
          esac
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
          echo "Resolved worker digest: $digest"

      - name: Configure kubeconfig
        if: env.RUN_ROW == 'true'
        env:
          KUBECONFIG_DATA: ${{ secrets.KUBECONFIG_PROD }}
        run: |
          set -euo pipefail
          if [ -z "${KUBECONFIG_DATA:-}" ]; then
            echo "::error::KUBECONFIG_PROD secret is not set; see docs/deploy.md"
            exit 1
          fi
          mkdir -p ~/.kube
          printf '%s' "$KUBECONFIG_DATA" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Helm upgrade --install (db chart)
        # CNPG Cluster lives in its own Helm release so its lifecycle is
        # decoupled from the api chart. Idempotent: on subsequent runs
        # with no value change, helm finds no diff and returns instantly.
        # No --wait: Helm 3 doesn't track CNPG Cluster's `Ready` condition,
        # so --wait would return as soon as the CRD is accepted (instant)
        # which proves nothing. The next step does the real wait via
        # kubectl against `.status.phase`.
        if: env.RUN_ROW == 'true' && matrix.db-chart-path != ''
        env:
          DB_RELEASE: ${{ matrix.db-release-name }}
          DB_CHART: ${{ matrix.db-chart-path }}
          DB_VALUES: ${{ matrix.db-values-file }}
        run: |
          set -euo pipefail
          helm upgrade --install "$DB_RELEASE" "$DB_CHART" \
            -n wordsparrow --create-namespace \
            -f "$DB_VALUES"

      - name: Wait for CNPG cluster Ready
        # Block the api install until the cluster is actually serving.
        # `.status.phase == "Cluster in healthy state"` is the CNPG phase
        # string emitted once primary election + replica join complete.
        # First-deploy bootstrap on Hetzner volumes is 5-10 min; steady-
        # state runs return instantly. 15 m covers the cold-boot worst case.
        if: env.RUN_ROW == 'true' && matrix.db-chart-path != ''
        env:
          DB_RELEASE: ${{ matrix.db-release-name }}
        run: |
          set -euo pipefail
          kubectl wait \
            --for=jsonpath='{.status.phase}'='Cluster in healthy state' \
            "cluster.postgresql.cnpg.io/$DB_RELEASE" \
            -n wordsparrow \
            --timeout=15m

      - name: Helm upgrade --install (api chart)
        if: env.RUN_ROW == 'true'
        id: helm
        env:
          DIGEST: ${{ steps.digest.outputs.digest }}
          # Empty for rows without a worker (game); the conditional `--set`
          # below skips the flag in that case.
          WORKER_DIGEST: ${{ steps.worker-digest.outputs.digest }}
          RELEASE: ${{ matrix.release-name }}
          CHART: ${{ matrix.chart-path }}
          VALUES: ${{ matrix.values-file }}
          # Identity-only: the maintainer UUID(s) are injected at deploy rather than committed.
          MAINTAINER_USER_IDS: ${{ matrix.context == 'identity' && secrets.MAINTAINER_USER_IDS || '' }}
        # `--atomic` was dropped on purpose: when a release fails,
        # `--atomic` rolls back BEFORE the diagnostics step can run, so
        # the broken pods (and their logs / events) are gone by the time
        # we'd otherwise capture them. We keep `--wait --timeout 5m` so
        # the step still surfaces the failure as a non-zero exit, but
        # leave the pending-upgrade release in place.
        #
        # The db-chart + kubectl-wait steps above guarantee CNPG is
        # serving before this step starts, so the wait-for-postgres
        # initContainer in the api chart completes in <1 s in steady
        # state. The 5 m budget covers Flyway migrations + JVM warm-up;
        # no headroom needed for cluster bootstrap.
        run: |
          set -euo pipefail
          # Build the helm args incrementally so the worker `--set` is
          # only injected for contexts that ship one (ADR-0042 grid
          # ensure-dailies CronJob); otherwise the flag would still
          # apply harmlessly but we keep the command surface minimal.
          set -- helm upgrade --install "$RELEASE" "$CHART" \
            -n wordsparrow --create-namespace \
            -f "$VALUES" \
            --set image.digest="$DIGEST"
          if [ -n "$WORKER_DIGEST" ]; then
            set -- "$@" --set "ensureDailies.image.digest=$WORKER_DIGEST"
          fi
          # Inject identity maintainer UUID(s) only when the secret is set, keeping the privileged id out of git.
          if [ -n "${MAINTAINER_USER_IDS:-}" ]; then
            set -- "$@" --set "maintainerRoleBootstrap.userIds=$MAINTAINER_USER_IDS"
          fi
          # helm v4.2.1 'watcher' wait burns the full --timeout on each before-hook-creation hook deletion (helm/helm#32214); 'legacy' avoids it while still waiting for the rollout
          set -- "$@" --wait=legacy --timeout 5m
          "$@"

      - name: Capture failure diagnostics
        # Only runs when the helm step failed. `set +e` so we don't
        # short-circuit on missing resources. Output is grouped so the
        # GHA log UI surfaces collapsible sections.
        if: failure() && env.RUN_ROW == 'true' && steps.helm.conclusion == 'failure'
        env:
          RELEASE: ${{ matrix.release-name }}
        run: |
          set +e
          echo "::group::helm history (last 5 revisions)"
          helm history "$RELEASE" -n wordsparrow --max 5
          echo "::endgroup::"

          echo "::group::helm status"
          helm status "$RELEASE" -n wordsparrow --show-resources
          echo "::endgroup::"

          echo "::group::pod summary"
          kubectl get pods -n wordsparrow -l app.kubernetes.io/instance="$RELEASE" -o wide
          echo "::endgroup::"

          echo "::group::pod descriptions (events + probe failures)"
          kubectl describe pods -n wordsparrow \
            -l app.kubernetes.io/instance="$RELEASE"
          echo "::endgroup::"

          # Logs from current AND previous container — CrashLoopBackOff
          # rotates the active container, leaving its in-process logs
          # empty; --previous pulls the last-crash output where the
          # actual exception lives.
          echo "::group::pod logs (current + previous, last 500 lines)"
          for pod in $(kubectl get pods -n wordsparrow \
            -l app.kubernetes.io/instance="$RELEASE" -o name); do
            echo "=== $pod (current) ==="
            kubectl logs -n wordsparrow "$pod" --all-containers --tail=500
            echo "=== $pod (previous) ==="
            kubectl logs -n wordsparrow "$pod" --all-containers --tail=500 --previous
          done
          echo "::endgroup::"

          echo "::group::namespace events (last 50, oldest first)"
          kubectl get events -n wordsparrow \
            --sort-by=.lastTimestamp | tail -50
          echo "::endgroup::"

      - name: Rollback failed release
        # Replicates `--atomic`'s rollback now that diagnostics are
        # captured. Picks the prior good revision from `helm history`
        # and rolls back synchronously. If there is no prior revision
        # (very first install), we uninstall instead so the namespace
        # doesn't keep a permanently-failed release lying around.
        if: failure() && env.RUN_ROW == 'true' && steps.helm.conclusion == 'failure'
        env:
          RELEASE: ${{ matrix.release-name }}
        run: |
          set -euo pipefail
          history_json=$(helm history "$RELEASE" \
            -n wordsparrow -o json 2>/dev/null || echo '[]')
          prev_rev=$(echo "$history_json" | \
            jq -r '[.[] | select(.status=="deployed" or .status=="superseded")] | sort_by(.revision) | last | .revision // empty')
          if [ -n "$prev_rev" ]; then
            echo "Rolling back to revision $prev_rev"
            helm rollback "$RELEASE" "$prev_rev" \
              -n wordsparrow --wait=legacy --timeout 2m
          else
            echo "No prior good revision; uninstalling failed release"
            helm uninstall "$RELEASE" -n wordsparrow --wait=legacy --timeout 2m
          fi

      - name: Verify rollout
        if: env.RUN_ROW == 'true'
        env:
          RELEASE: ${{ matrix.release-name }}
        # `fullnameOverride` in each chart's values-prod.yaml pins the
        # deployment name to the release name, so this resolves
        # `deployment/wordsparrow-api` and `deployment/wordsparrow-game-api`.
        run: |
          set -euo pipefail
          kubectl -n wordsparrow rollout status deployment/"$RELEASE" --timeout=2m
          kubectl -n wordsparrow get pods -l app.kubernetes.io/instance="$RELEASE" -o wide

      - name: Smoke test /v1/health
        if: env.RUN_ROW == 'true'
        env:
          HEALTH_URL: ${{ matrix.health-url }}
        # External smoke test against the public ingress. cert-manager
        # + external-dns must already have the cert + DNS in place.
        # Five attempts at 10s apart absorbs DNS / cache propagation
        # jitter without masking a real outage — total budget 50s.
        run: |
          set -euo pipefail
          for i in 1 2 3 4 5; do
            if curl -sf -m 5 "$HEALTH_URL"; then
              echo "healthy"
              exit 0
            fi
            sleep 10
          done
          echo "::error::$HEALTH_URL did not return 200 after 50s"
          exit 1

      - name: CORS preflight smoke
        if: env.RUN_ROW == 'true'
        env:
          PREFLIGHT_URL: ${{ matrix.health-url }}
        # Runtime guard on ADR-0048: source-level Konsist can't prove the rolled-out pod's wildcard predicate is live; this verifies it against the public ingress.
        run: |
          set -euo pipefail
          deadline=$(( $(date +%s) + 60 ))
          attempt=0
          while :; do
            attempt=$((attempt + 1))
            headers_file="$(mktemp)"
            status=$(curl -sS -o /dev/null -D "$headers_file" -m 5 \
              -w '%{http_code}' \
              -X OPTIONS "$PREFLIGHT_URL" \
              -H 'Origin: https://wordsparrow.io' \
              -H 'Access-Control-Request-Method: GET' \
              -H 'Access-Control-Request-Headers: traceparent' \
              || true)
            allow_origin=$(grep -i '^access-control-allow-origin:' "$headers_file" \
              | tr -d '\r' | awk -F': ' '{print tolower($2)}' || true)
            allow_headers=$(grep -i '^access-control-allow-headers:' "$headers_file" \
              | tr -d '\r' | awk -F': ' '{print tolower($2)}' || true)
            if [ "$status" = "200" ] \
               && [ "$allow_origin" = "https://wordsparrow.io" ] \
               && printf '%s' "$allow_headers" | grep -q 'traceparent'; then
              echo "CORS preflight OK on attempt $attempt"
              rm -f "$headers_file"
              exit 0
            fi
            echo "::warning::attempt $attempt failed (status=$status allow_origin='$allow_origin' allow_headers='$allow_headers')"
            cat "$headers_file" || true
            rm -f "$headers_file"
            if [ "$(date +%s)" -ge "$deadline" ]; then
              break
            fi
            sleep 5
          done
          echo "::error::CORS preflight against $PREFLIGHT_URL failed within 60s — ADR-0048 wildcard predicate may not be live on the rolled-out pod"
          exit 1
