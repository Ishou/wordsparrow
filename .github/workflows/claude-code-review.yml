name: Claude Code Review

# Implements ADR-0001 §6a (2026-04-26 amendment): cycle review/fix on each
# PR until the latest bot review converges on `LGTM`, capped at 5 total
# bot reviews to prevent infinite loops from reviewer/fixer disagreement.
#
# Triggers fire on opened, ready_for_review, synchronize, and reopened so
# every PR head receives a definitive `claude-review` check status. The
# fixer's commit-and-push naturally produces a synchronize event, which
# re-runs this workflow and drives the next cycle. The cap is enforced by
# the "Check for convergence" step:
#   - If the latest [bot] review's first line is `LGTM`, skip (converged).
#   - If total [bot] reviews >= 5, skip with a warning so a human can
#     intervene (max-iteration cap).
#   - Otherwise (first cycle, or non-LGTM with count < 5), continue.
#
# Why this matters: the previous "exactly one pass" rule produced a single
# review and a single fix attempt, with no mechanism to confirm the fix
# actually addressed the findings. Cycling lets the reviewer verify the
# fixer's work and lets the fixer iterate on residual findings; the
# 5-iteration cap bounds the worst case (reviewer/fixer disagreement
# requiring human arbitration).
#
# Concurrency keys on the PR number with cancel-in-progress=false (default):
# arrivals while a cycle is running queue rather than restart, so each
# iteration sees the prior reviews and the cap evaluates deterministically.
#
# Job sequence (each cycle):
#   1. Reviewer agent reads CLAUDE.md/MANIFESTO.md/ADRs and the PR diff,
#      then submits one review via `gh pr review --comment`.
#   2. Detect step queries the just-posted review and sets `has_findings`.
#   3. Fixer agent (only when findings exist) reads the review, applies
#      fixes or writes rebuttals, runs the relevant verification, commits,
#      pushes to the PR branch, and posts one reply comment. The push fires
#      a synchronize event that starts the next cycle.
#
# Permissions: `contents: write` is required only for the fixer to push to
# PR branches. The reviewer's allowlist confines it to read + post-review;
# the fixer's allowlist is broader (Edit/Write/Bash). The 5-iteration cap
# bounds total work per PR.

on:
  pull_request:
    types: [opened, ready_for_review, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  claude-review:
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: read
      id-token: write
      actions: read

    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout PR head
        # GitHub's `GITHUB_TOKEN` cannot carry the `workflows` permission —
        # that scope is only available to fine-grained PATs and GitHub Apps.
        # When the §6a fixer's diff includes any `.github/workflows/*.yml`
        # change, a push authenticated by `GITHUB_TOKEN` is rejected with
        # "refusing to allow a GitHub App to create or update workflow ...
        # without `workflows` permission" (observed on PR #18, #35).
        #
        # Fix: thread `CLAUDE_BOT_PAT` through the checkout step. The token
        # configured here is persisted into the local git config's
        # `extraheader` and inherited by every subsequent `git push` in the
        # job — including the fixer's. Fall back to `GITHUB_TOKEN` when the
        # secret isn't set so this workflow keeps working on PRs opened
        # before the secret has been provisioned. See the PR that introduced
        # this comment for PAT setup instructions.
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
          token: ${{ secrets.CLAUDE_BOT_PAT || secrets.GITHUB_TOKEN }}

      - name: Configure git identity for fixer commits
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Check for convergence (cycle-until-LGTM, max 5 iterations)
        id: cap
        # Cycle behavior (per ADR-0001 §6a 2026-04-26 amendment + 2026-05-25):
        # - If the latest [bot] review's first line is `LGTM`, skip
        #   (convergence reached; no further cycles).
        # - If total [bot] reviews >= 5, skip with a warning so a human
        #   can intervene (max-iteration cap, prevents infinite loops
        #   from reviewer/fixer disagreement).
        # - If the latest two [bot] reviews flag the same first finding
        #   (identical first-finding heading after the `Findings —`
        #   first line), skip with a warning — the loop is stuck on a
        #   carry-over the fixer cannot or should not resolve by code
        #   change. The dispatch skill's 3c-loop-terminator pattern
        #   handles the next move (typically: cite the §4 soft-target
        #   override in the PR body, dispatch a manual reviewer for
        #   fresh verdict, then merge).
        # - Otherwise (first cycle, or non-LGTM review with count < 5
        #   and findings have changed), continue: run reviewer + fixer.
        #
        # Synchronize fires after the fixer's push, naturally driving the
        # next cycle. Cap step gates: convergence (LGTM in latest bot
        # review) OR 5-iteration max OR identical-finding loop.
        run: |
          set -euo pipefail
          reviews=$(gh pr view "$PR_NUMBER" --json reviews \
            --jq '[.reviews[] | select(.author.login | endswith("[bot]"))]' \
            || echo '[]')
          count=$(echo "$reviews" | jq 'length')

          if [ "${count:-0}" -eq 0 ]; then
            echo "::notice::No prior §6a review; first cycle."
            echo "skip=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "${count:-0}" -ge 5 ]; then
            echo "::warning::Hit 5-iteration cap on PR $PR_NUMBER; skipping further §6a cycles. Human intervention required."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Read latest bot review's first line; convergence on `LGTM`.
          first_line=$(echo "$reviews" | jq -r '.[-1].body // ""' | head -n 1)
          echo "Latest bot-review first line: $first_line"
          if echo "$first_line" | grep -q '^LGTM'; then
            echo "::notice::Latest §6a review is LGTM; convergence reached, skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Identical-finding-loop guard (2026-05-25): if the latest two
          # bot reviews have the same first Finding heading, the loop is
          # stuck. Skip and warn — the orchestrator (or maintainer) takes
          # the next move out-of-band.
          #
          # Heuristic: extract the line starting with `## Finding 1 —`
          # (or `## Finding 1` then a dash) from each review body and
          # compare. If they match after stripping any `[carry-over from
          # cycle N]` cycle-number references, that's the loop signal.
          if [ "${count:-0}" -ge 2 ]; then
            latest_first_finding=$(echo "$reviews" | jq -r '.[-1].body // ""' \
              | grep -m1 -E '^## Finding 1' \
              | sed -E 's/\[carry-over from cycle [0-9]+\]//g' \
              | sed -E 's/[[:space:]]+/ /g' \
              | sed -E 's/^[[:space:]]+|[[:space:]]+$//g' || echo "")
            prior_first_finding=$(echo "$reviews" | jq -r '.[-2].body // ""' \
              | grep -m1 -E '^## Finding 1' \
              | sed -E 's/\[carry-over from cycle [0-9]+\]//g' \
              | sed -E 's/[[:space:]]+/ /g' \
              | sed -E 's/^[[:space:]]+|[[:space:]]+$//g' || echo "")
            if [ -n "$latest_first_finding" ] && [ "$latest_first_finding" = "$prior_first_finding" ]; then
              echo "::warning::Identical first Finding on cycles $((count-1)) and $count for PR $PR_NUMBER ('${latest_first_finding}'); auto-loop terminated. Orchestrator or maintainer handles next move (cite §4 soft-target override OR escalate)."
              echo "skip=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          fi

          echo "::notice::Latest §6a review has findings ($count prior cycles); continuing."
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Run reviewer agent
        if: steps.cap.outputs.skip != 'true'
        id: reviewer
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Required so cycle-until-LGTM (per ADR-0001 §6a 2026-04-26 amendment) keeps
          # running on synchronize events fired by the fixer's own push (actor =
          # claude[bot]). Default action policy refuses bot-initiated runs.
          allowed_bots: 'claude[bot],github-actions[bot]'
          additional_permissions: |
            actions: read
          # Read-only on the repo. The reviewer's only output is a markdown
          # file at /tmp/review.md — the workflow's "Submit review" step
          # below posts it via `gh pr review`. The agent has NO `gh pr review`
          # capability on its tool allowlist, which mechanically prevents the
          # multi-post behaviour observed on PR #31 (12 spurious reviews).
          # `Write` is required so the agent can create /tmp/review.md —
          # without it the agent silently no-ops the file step and the
          # "Submit review" fallback posts a hollow `LGTM, no findings.`
          # The earlier `cat <<'BODY' > /tmp/review.md` heredoc form did not
          # work under `Bash(cat:*)` because the redirect (`>`) is parsed
          # outside the `cat` argv the allowlist matches against.
          claude_args: '--allowed-tools "Read,Glob,Grep,Write,Bash(gh pr view:*),Bash(gh pr diff:*),Bash(cat:*),Bash(test:*)"'
          prompt: |
            You are the §6a reviewer agent for PR #${{ github.event.pull_request.number }}
            on the bliss repository (${{ github.repository }}).

            **Invoke the `/reviewer` skill first.** It encodes your role,
            scope, output format, and how to submit (CI mode: write to
            `/tmp/review.md`; the workflow's "Submit review" step posts
            it via `gh pr review`). Then also invoke the relevant domain
            playbook for the conventions specific to this PR's files:

            - `/schemas` if the PR touches `**/openapi.yaml` or
              `**/asyncapi.yaml`.
            - `/frontend` if the PR touches `frontend/**`.
            - `/jvm-backend` if the PR touches `grid/**`, `game/**`, or
              `*.gradle.kts`.

            ## PR-specific bits

            1. `gh pr view "$PR_NUMBER" --json title,body,files,additions,deletions`
               and `gh pr diff "$PR_NUMBER"` to see the change.
            2. Read prior reviews on this PR with
               `gh pr view "$PR_NUMBER" --json reviews`. For each prior
               bot review, build a checklist of every finding it raised.
               Then check the current diff against each:
                 - **Resolved** — note it near the top of your review as
                   `Resolved: Cycle N · Finding M — <summary>`. Don't
                   re-flag.
                 - **Still open** — re-flag with
                   `## Finding K — [carry-over from cycle N] <summary>`.
                   The fixer cannot claim ignorance of carry-overs.
                 - **Re-opened** — the fix introduced a new defect
                   against the same rule (e.g. `perf:` → `style:`
                   on PR #368). Flag explicitly: name both occurrences.
               Don't take the fixer's reply comment at face value —
               grep the diff for the offending pattern and decide on
               evidence. PRs #356, #361, #368, #401 each cycled because
               the fixer's reply over-promised relative to the diff.
            3. Read CLAUDE.md, MANIFESTO.md, and all merged ADRs under
               `docs/adr/` (especially ADR-0001 §6a). Read any
               `<context>/api/openapi.yaml` or `asyncapi.yaml` relevant
               to the files changed.
            4. Compose the review body. First line is `LGTM, no findings.`
               or `Findings — see comments below.` per the skill. List
               carry-overs first, then new findings.
            5. Write to `/tmp/review.md` and exit. The "Submit review"
               step posts it. You have no `gh pr review` capability; do
               not try to post yourself.

            ## When the issue is the PR description, not the diff

            If a prior reviewer's finding has been addressed in the
            diff but the PR body still claims the old state (e.g. the
            body still says "this PR will go over 400 lines, splitting
            into follow-up" *after* the split already happened), flag
            it as a PR-body update, not a code finding. PR #358 cycle 4
            burned on this. Phrasing: "PR body still claims X; the
            current diff no longer shows X. Update or remove the claim."

            ## 400-line target is SOFT (ADR-0001 §4 amended 2026-05-25)

            The 400-line figure is a target, not a hard cap. Do NOT
            re-flag a cap finding in either of these cases:

            1. PR body cites the override. If the PR body cites the
               §4 2026-05-25 soft-target amendment, the orchestration
               procedure's Standing maintainer authorization section,
               or names exception category (a / b / c / coherent
               layer) per the §4 amendments — treat the cap as
               resolved. Do not re-flag in this or subsequent cycles
               while the citation stays in the body.

            2. Carry-over without material change. If the prior
               cycle flagged the cap with the same rule and same
               proposed split, AND the diff has materially changed
               since (a fix commit landed, or the PR body was
               edited to cite the override), do not re-flag the cap.
               Re-flagging here triggers the dispatch skill 3c
               loop-terminator and is treated as a stuck loop.

            If the cap genuinely remains a problem after a body edit
            cycle (e.g. PR ballooned past 1500 lines and the citation
            is not in good faith), flag it once with explicit
            reference to the prior cycle and the specific reason the
            citation is invalid — do not silently repeat the same
            generic cap-flag.

            ## Cap

            This is your only pass per cycle. The fixer runs after you
            and pushes once, which fires the next cycle. The pre-flight
            "Check for convergence" step gates further cycles when the
            latest review is `LGTM`, 5 cycles have run, OR the
            identical-finding loop guard fires.

      - name: Submit review
        # The reviewer agent above writes its review to /tmp/review.md and
        # has no `gh pr review` capability. This step posts the file once,
        # bash-side, mechanically preventing the multi-post behaviour seen
        # on PR #31 where an in-prompt agent iterated 12 times to figure
        # out gh-CLI escaping. If the agent didn't write the file (it
        # crashed, the model timed out, etc.), default to a non-blocking
        # placeholder so the PR isn't left without a review.
        if: steps.cap.outputs.skip != 'true'
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ ! -s /tmp/review.md ]; then
            echo "::warning::Reviewer agent did not produce /tmp/review.md; posting fallback notice."
            cat > /tmp/review.md <<'BODY'
          LGTM, no findings.

          (The §6a reviewer agent did not produce a review body. This is
          a workflow-level fallback so the PR isn't left without a check.
          Tracked via the action run logs.)
          BODY
          fi
          gh pr review "$PR_NUMBER" --comment --body-file /tmp/review.md

      - name: Detect findings
        if: steps.cap.outputs.skip != 'true'
        id: detect
        run: |
          set -euo pipefail
          # Read the review body directly from /tmp/review.md — the same
          # file the "Submit review" step posted moments earlier. The
          # previous implementation queried `gh pr view --json reviews`
          # immediately after the submit, which raced GitHub's API
          # propagation delay: the just-posted review was frequently
          # absent from the response, leaving `body` empty, flipping
          # `has_findings=false`, and silently skipping the fixer. The
          # 2026-04-26 deploy session hit this on at least 6 consecutive
          # PRs. The file content is the source of truth — it's what
          # was posted — and reading it is deterministic and synchronous.
          if [ ! -s /tmp/review.md ]; then
            echo "::warning::No /tmp/review.md found; skipping fixer."
            echo "has_findings=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          first_line=$(head -n 1 /tmp/review.md)
          echo "First line of review: $first_line"
          # Match `LGTM` at the start, case-sensitive (per the prompt).
          if echo "$first_line" | grep -q '^LGTM'; then
            echo "Reviewer wrote LGTM — skipping fixer."
            echo "has_findings=false" >> "$GITHUB_OUTPUT"
          else
            echo "Reviewer posted findings — running fixer."
            echo "has_findings=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Detect failing CI checks (non-excluded)
        # Polls the PR's check rollup once; flags non-excluded failures
        # so the fixer runs even when the code-review verdict is LGTM
        # but a build/lint/test check is red.
        #
        # Excluded checks (handled out-of-band, not by this workflow):
        #   dco, commitlint, branch-name — the human maintainer manages
        #     these (signoff trailer, conventional-commit type/scope,
        #     branch-prefix rename). The fixer should not touch them.
        #   claude-review — this workflow itself; self-referential.
        #   CodeQL, Analyze (java-kotlin) — informational, not a merge gate.
        #
        # CAVEAT — race with parallel CI: the build/spectral/regen-and-diff
        # workflows start in parallel with this one on the same `opened` /
        # `synchronize` event. By the time this step runs they may still
        # be IN_PROGRESS, and `ci_red` will be false this cycle. The next
        # synchronize event (typically the fixer's own push) gives another
        # chance to react. A future workflow_run trigger could close that
        # gap, but is out of scope here.
        if: steps.cap.outputs.skip != 'true'
        id: detect-ci
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          rollup=$(gh pr view "$PR_NUMBER" --json statusCheckRollup --jq '.statusCheckRollup // []')
          failed=$(echo "$rollup" | jq -r '
            [.[]
             | { name: (.name // .context // ""),
                 conc: ((.conclusion // .state // "") | ascii_upcase) }
             | select(.conc == "FAILURE")
             | .name]
            | map(select(. != ""))
            | map(select(test("^(dco|commitlint|branch-name|claude-review|CodeQL.*|Analyze.*)$") | not))
            | unique
            | join(",")
          ')
          if [ -n "$failed" ]; then
            echo "::notice::Failing non-excluded CI checks: $failed"
            echo "ci_red=true" >> "$GITHUB_OUTPUT"
            echo "failed_checks=$failed" >> "$GITHUB_OUTPUT"
          else
            echo "ci_red=false" >> "$GITHUB_OUTPUT"
            echo "failed_checks=" >> "$GITHUB_OUTPUT"
          fi

      - name: Run fixer agent
        if: steps.cap.outputs.skip != 'true' && (steps.detect.outputs.has_findings == 'true' || steps.detect-ci.outputs.ci_red == 'true')
        uses: anthropics/claude-code-action@v1
        env:
          # Surfaced into the agent's environment so the prompt below can
          # reference $CI_FAILED_CHECKS and the agent can act on it.
          CI_FAILED_CHECKS: ${{ steps.detect-ci.outputs.failed_checks }}
          HAS_REVIEW_FINDINGS: ${{ steps.detect.outputs.has_findings }}
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Required so cycle-until-LGTM (per ADR-0001 §6a 2026-04-26 amendment) keeps
          # running on synchronize events fired by the fixer's own push (actor =
          # claude[bot]). Default action policy refuses bot-initiated runs.
          allowed_bots: 'claude[bot],github-actions[bot]'
          additional_permissions: |
            actions: read
          # The fixer needs Edit/Write to apply fixes, plus shell access to
          # run pnpm/gradle, commit, push, and post the reply comment. The
          # one-push cap is enforced by the trigger config (synchronize is
          # not a trigger), not by this allowlist.
          #
          # `Bash(gh run:*)` lets the fixer pull failed-CI logs via
          # `gh run list` / `gh run view --log-failed`. `Bash(jq:*)` is
          # used to parse the run listing.
          claude_args: >-
            --allowed-tools "Read,Glob,Grep,Edit,Write,Bash(git:*),Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr edit:*),Bash(gh run:*),Bash(jq:*),Bash(pnpm:*),Bash(npm:*),Bash(npx:*),Bash(node:*),Bash(./gradlew:*),Bash(gradle:*),Bash(cat:*),Bash(ls:*),Bash(test:*),Bash(true)"
          prompt: |
            You are the §6a fixer agent for PR #${{ github.event.pull_request.number }}
            on the bliss repository (${{ github.repository }}).

            You may be running for one or both of two reasons:
              1. **Review findings** — `HAS_REVIEW_FINDINGS=$HAS_REVIEW_FINDINGS`.
                 If `true`, the §6a reviewer just posted findings on
                 this PR; apply fixes / rebuttals.
              2. **Failing CI checks** — `CI_FAILED_CHECKS=$CI_FAILED_CHECKS`.
                 If non-empty, those checks are RED on this PR HEAD.
                 Diagnose and fix them too (see "## CI failures" below).
                 The list is comma-separated; `dco`, `commitlint`, and
                 `branch-name` are explicitly excluded — DO NOT touch
                 those checks even if you notice them failing.

            Your job is to apply the fixes (or post rebuttals) and push the
            result, exactly once.

            **Invoke the relevant domain skill before applying fixes.** It
            encodes the conventions and gotchas you're likely to hit:

            - `/schemas` if the PR touches `**/openapi.yaml` or
              `**/asyncapi.yaml`.
            - `/frontend` if the PR touches `frontend/**`.
            - `/jvm-backend` if the PR touches `grid/**`, `game/**`, or
              `*.gradle.kts`.

            ## Read first

            - `CLAUDE.md` (rules) and `MANIFESTO.md` (rationale).
            - All merged ADRs under `docs/adr/`.
            - The reviewer's most recent review on this PR. Get the body
              with:
              `gh pr view "$PR_NUMBER" --json reviews --jq 'last(.reviews[] | select(.author.login | endswith("[bot]") or . == "claude")).body'`

            ## Your role

            You perform exactly **one** fix pass on this PR. After you push,
            the workflow does not re-trigger; the human maintainer reviews
            and merges. No second review, no second fix, no agent
            ping-pong.

            ## Procedure

            1. Identify each finding from the review. The reviewer was
               instructed to format findings with rule citations and
               file/line refs. Treat each as a separate item, including
               any marked `[carry-over from cycle N]` — these were open
               in a prior cycle and the prior fixer did not close them.

               **Also pull every prior bot review's findings** (not just
               the latest one) and confirm each is either:
               - explicitly marked `Resolved` in the latest review, OR
               - addressed by your current changes.
               If a prior finding is neither marked resolved nor in your
               fix list, treat it as still open. PRs #330, #356, #361,
               #368, #401, #406 all cycled because prior findings drifted
               out of view between cycles.

            2. For each finding, do exactly one of:
               - **Apply the fix** with Edit/Write tools.
               - **Note as rebuttal** in your reply comment, with a brief
                 reason grounded in the manifesto / ADRs.
               - **Defer** with `out of scope, see follow-up workstream <id>`
                 (only when the fix would push the PR over the 400-line cap
                 or would require a separate context).

               Use judgment when the reviewer's literal instruction is
               technically wrong: fix the underlying issue, not the literal
               text. Explain non-trivial decisions in your reply comment.

               **Don't introduce a fresh defect against the same rule.**
               PR #368 cycle 2 fixed `perf:` commits by amending some of
               them, then added a `style:` commit — same `type-enum` rule
               violated, fresh occurrence. If a rule fired in a prior
               cycle, re-check your new commits against that rule before
               pushing. The closed set of disallowed commit types is
               `[perf, style, build, ci, revert]`; allowed is
               `[feat, fix, chore, refactor, test, docs]`.

               If `$CI_FAILED_CHECKS` is non-empty, also treat each
               failing check as a finding and address it per the
               "Handling failing CI checks" section below.

            3. **Verify** based on what you changed:
               - If you touched `frontend/`:
                 `cd frontend && pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint && pnpm test && pnpm build`
               - If you touched JVM (anything under `grid/`, `build.gradle.kts`,
                 `settings.gradle.kts`, etc.): `./gradlew build`
               - If you only touched docs/ADRs: read the diff with `git diff`
                 for coherence; no executable verification needed.
               - Run only what is relevant to the changed files.
               - If verification fails, fix the cause; do not work around.

            4. Read your diff: `git diff`. Sanity-check before committing.

            5. Commit with focused conventional-commit messages. One commit
               if changes are coupled; split when independent. Use
               `<type>(<scope>): ...` form, scope = bounded-context-layer
               or `adr-NNNN`. Examples:
               - `fix(frontend-ui): set document title for WCAG 2.4.2 compliance`
               - `docs(adr-0004): require IaC for Cloudflare Pages project`
               - `fix(grid-domain): handle zero-length word edge case`

               Do **not** modify `CLAUDE.md`, `MANIFESTO.md`, or any merged
               ADR.

            6. Push to the PR branch:
               `git push origin "$PR_HEAD_REF"`
               Do **not** force-push. Do **not** push to main. The branch
               is already checked out at HEAD.

               **Before pushing, scan your new commits' subjects for
               disallowed types.** `git log origin/main..HEAD --pretty='%s'`
               and grep for `^(perf|style|build|ci|revert)\(`. If any
               match, amend before push — commitlint will block merge
               otherwise and trigger another review cycle.

            7. **Write your reply to `/tmp/reply.md`** using the Write
               tool. Do **not** use a `cat <<'REPLY' > /tmp/reply.md`
               heredoc: the redirect (`>`) is parsed outside the `cat`
               argv that `Bash(cat:*)` matches, so the file write
               silently no-ops under the allowlist. This is the same
               bug the reviewer step previously had — see the comment
               on the reviewer's `--allowed-tools` line above.
               That is your only output for this finding-mapping step.
               **Do not** post the comment yourself — you have no
               `gh pr comment` capability. A separate workflow step
               submits the file once, bash-side. If `/tmp/reply.md`
               doesn't exist when this agent step exits, no comment is
               posted (no-op).
               Format: short bullet list, each finding marked
               `fixed in <short-sha>` or with a brief rebuttal. Cite the
               rule and the lines you changed. Tight, no filler.

            ## Handling failing CI checks

            When `$CI_FAILED_CHECKS` is non-empty, each comma-separated
            name is a GitHub Actions check at FAILURE on this PR's HEAD.
            The list has already been filtered for excluded checks
            (`dco`, `commitlint`, `branch-name`, `claude-review`,
            `CodeQL.*`, `Analyze.*`) — DO NOT modify those even if you
            notice them failing.

            For each entry in `$CI_FAILED_CHECKS`:

            1. Find the run ID for that check on the current branch:
               ```
               gh run list --branch "$PR_HEAD_REF" \
                   --json databaseId,name,conclusion --limit 30 \
                 | jq -r --arg n "<check-name>" \
                     '.[] | select(.name==$n and .conclusion=="failure") | .databaseId' \
                 | head -1
               ```
            2. Pull the failed-step log:
               `gh run view <run-id> --log-failed`. Focus on the first
               real error — later lines are usually cascade. If the
               log is huge, pipe to `tail -200` to surface the actual
               exception faster.
            3. Diagnose the root cause and apply the fix. Common shapes:
               - `build` (gradle): compile / Spotless / Konsist / test
                 failure. Reproduce with
                 `./gradlew :<scope>:check :<scope>:spotlessCheck`.
               - `build` (frontend): typecheck / lint / test / build
                 failure. Reproduce with
                 `cd frontend && pnpm typecheck && pnpm lint && pnpm test && pnpm build`.
               - `regen-and-diff`: stale generated types. Run
                 `cd frontend && pnpm api:generate` and commit the diff.
               - `spectral`: schema lint. Run
                 `npx -y @stoplight/spectral-cli@latest lint <file>`
                 with the appropriate ruleset.
               - `helm-lint` / `api-chart-lint`: `helm lint` against
                 the chart directory.
            4. If the failure is genuinely a CI flake (transient
               network, runner timeout) and not a real defect, note
               that in your reply rather than pushing a no-op fix.
               The maintainer can re-run.

            CI fixes are bundled into the same single push as
            code-review fixes. Use a focused conventional-commit scope
            for the area you actually changed (e.g. `fix(grid-api): …`,
            not `fix(ci): …`). Remember the disallowed types:
            `perf|style|build|ci|revert` — use `refactor:` or `chore:`.

            ## Handling PR title/body findings

            The reviewer may flag findings that live in the PR title or
            body rather than the diff (e.g. "PR body still claims X;
            the current diff no longer shows X" — see PR #358 cycle 4).
            For those, edit the PR metadata directly with `gh pr edit`
            instead of (or in addition to) changing files:

            - Title: `gh pr edit "$PR_NUMBER" --title "<new title>"`
            - Body:  write the new body to `/tmp/pr-body.md` with the
              Write tool, then
              `gh pr edit "$PR_NUMBER" --body-file /tmp/pr-body.md`.
              Do not pass the body inline via `--body "..."` — it
              mangles newlines and breaks markdown.

            Edit the minimum needed to resolve the finding; preserve
            the rest of the body verbatim (fetch the current body
            first with `gh pr view "$PR_NUMBER" --json body --jq .body`).
            PR-metadata edits do NOT produce a commit, so note them in
            your `/tmp/reply.md` as `pr-body updated` / `pr-title
            updated` rather than `fixed in <sha>`. They also do not
            fire a `synchronize` event, so if you ONLY edit the PR
            body/title and push nothing else, the workflow will not
            re-trigger — that is expected, since the next reviewer
            pass would just re-read the same metadata.

            ## Constraints

            - Do not force-push.
            - Do not modify CLAUDE.md, MANIFESTO.md, or any merged ADR.
            - Do not skip CI hooks (no `--no-verify`, no `--no-gpg-sign`).
            - Do not fix things not in the review (no scope creep).
            - Do not call `gh pr comment` more than once.
            - Do not open a new PR.
            - Do not push to any branch other than `$PR_HEAD_REF`.

            ## Cap

            This is your only pass. After your push, the workflow does not
            re-trigger.

      - name: Submit fixer reply
        # The fixer agent above writes its findings→fixes mapping to
        # /tmp/reply.md and has no `gh pr comment` capability. Same shape
        # as the "Submit review" step: bash-side post, called exactly once,
        # immune to in-prompt iteration loops. Skipped if the agent
        # didn't produce a file (e.g. no findings, or agent crashed).
        if: steps.cap.outputs.skip != 'true' && (steps.detect.outputs.has_findings == 'true' || steps.detect-ci.outputs.ci_red == 'true')
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ ! -s /tmp/reply.md ]; then
            echo "::warning::Fixer agent did not produce /tmp/reply.md; skipping reply comment."
            exit 0
          fi
          gh pr comment "$PR_NUMBER" --body-file /tmp/reply.md
