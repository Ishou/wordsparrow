.idea/
bliss.iml

# Gradle
build/
.gradle/
.kotlin/
!gradle/wrapper/gradle-wrapper.jar

# Claude Code: project-level skills, subagents, slash commands, and hooks under
# .claude/ are versioned. Ignore only the ephemeral and per-developer entries.
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Superpowers brainstorming session artefacts (mockups, server state).
.superpowers/

# design-sync (claude.ai/design): staged converter, build output, and
# machine state. Durable sync inputs under .design-sync/ (config.json,
# NOTES.md, conventions.md, previews/, overrides/, tsconfig.sync.json)
# ARE committed; only the ephemera below are ignored.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Helm: subchart tarballs are downloaded by `helm dependency update`
# and reproduced from Chart.lock; never commit them.
infra/*/charts/

# Python
__pycache__/
*.pyc

# Large/ephemeral data: external dumps, training artefacts
data/dbnary/
data/lora/*
data/lora_filter/
models/
checkpoints/
*.gguf
*.safetensors

# Licensed external corpora (Lexique3 CC BY-SA, etc.): stay local,
# never redistribute. ADR-0013 §1, ADR-0023.
data/external/

# Eval-iteration scratch CSVs that historically embedded verbatim
# DBnary glosses (CC BY-SA) in the `definition` / `synonyms` columns.
# Per ADR-0023 §"Distribution discipline" we don't redistribute DBnary
# text. Keep them local for offline analysis; never commit. The
# clean per-pipeline outputs in `data/eval/production/` (LoRA-only,
# no DBnary text) remain tracked.
data/eval/lemma_clues.csv
data/eval/lemma_clues.mistral-nemo.csv
data/eval/lemma_clues_iter[2-7].csv
data/eval/lemma_clues_run_*.csv
data/eval/run_v0_*.csv
data/eval/sample_100_enriched.csv
data/eval/sample_100_with_definitions.csv
data/eval/sample_iter[2-7]_enriched.csv
data/eval/sample_iter2_with_definitions.csv
data/eval/sample_run_*_enriched.csv
data/eval/sample_run_*_with_definitions.csv
data/eval/synonym_clues_iter[6-7].csv

# Grid-generation bench artefacts (machine-specific, not committed).
data/eval/grid_gen_bench_*.csv
data/eval/baseline_wave-*.csv
data/eval/current_wave-*.csv
grid/api/data/

# Modal-lane corpus build artefacts (regeneratable from manifest + inputs).
# `data/lora/*` above ignores everything under data/lora by default; we
# re-include only the manifest + (later) the build summary. The JSONL
# outputs and the gold-only smoke JSONL stay ignored.
!data/lora/modal_corpus_v1/
data/lora/modal_corpus_v1/train.jsonl
data/lora/modal_corpus_v1/val.jsonl
!data/lora/modal_corpus_v1/manifest.toml
data/seed/gold_pilot_v1_train.jsonl
data/seed/gold_pilot_v1_val.jsonl
