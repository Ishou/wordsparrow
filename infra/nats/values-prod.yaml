# Production overrides — Hetzner k3s (ADR-0009 §2).
image:
  # Pinned to the nats:2.10-alpine OCI image index digest resolved on
  # 2026-05-20. Refresh by re-running the registry digest-lookup against
  # the 2.10-alpine tag when bumping to a new minor.
  digest: "sha256:b83efabe3e7def1e0a4a31ec6e078999bb17c80363f881df35edc70fcb6bb927"
storage:
  size: 5Gi
  storageClass: "hcloud-volumes"
streamInit:
  image:
    # natsio/nats-box:0.19.7 OCI image index digest, resolved 2026-06-14.
    digest: "sha256:ffce8bd103383f179f8c7f11cf645726acf5d17280706c530c3b342dbe16334c"

# Prometheus exporter sidecar — on in prod; digest pinned to multi-arch image index for natsio/prometheus-nats-exporter:0.20.1.
metricsExporter:
  enabled: true
  image:
    digest: "sha256:4fbf6dacb84780a45a1c3af9b1080c69451a288d20902deae671b80717bb8f61"

# DLQ stream — on in prod (scaling-readiness per ADR-0049).
dlq:
  enabled: true
