apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "bliss-nats.fullname" . }}
  labels: {{- include "bliss-nats.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "bliss-nats.selectorLabels" . | nindent 6 }}
  policyTypes: [Ingress]
  ingress:
    # Only the bounded-context API pods (and the stream-init Job) can reach
    # the NATS port. Each pod carries selectorLabels matching its chart name.
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: bliss-identity-api } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: bliss-game-api } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: bliss-grid-api } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: bliss-survey-api } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: bliss-nats } }  # stream-init Job
      ports:
        - { protocol: TCP, port: {{ .Values.config.port }} }
{{- if .Values.metricsExporter.enabled }}
    # SigNoz collector (observability ns) → metrics port only (ADR-0027); client ingress rule above is unchanged.
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: observability } }
      ports:
        - { protocol: TCP, port: {{ .Values.metricsExporter.port }} }
{{- end }}
